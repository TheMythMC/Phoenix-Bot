version: '3'
services:
  extra:
    build:
      context: .
      dockerfile: Dockerfile
    image: bupachile/node12.16.1-alpine
    entrypoint: bash docker/launch.sh
    volumes:
      - .:/usr/src/extra
    container_name: phoenix-extra